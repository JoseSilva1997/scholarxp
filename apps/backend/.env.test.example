# Role: Template for an isolated test database so e2e cleanup never targets development data.
# Use a dedicated DB/schema name containing "test" or "e2e" to satisfy safety checks.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/scholarxp_test?schema=public

# Keep this as false so backend startup remains deterministic in e2e.
SKIP_PRISMA_TX=false
