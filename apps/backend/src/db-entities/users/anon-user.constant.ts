// Sentinel user row that owns content reassigned from deleted teachers (modules, invites).
// Seeded directly in the database; no AuthIdentity, so it cannot be logged into.
export const ANON_USER_ID = 1;
