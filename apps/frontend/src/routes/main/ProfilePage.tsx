// Placeholder for account/profile management; uses shared page shell for consistent layout.
import MainSection from '../../components/MainSection';

export default function ProfilePage() {
  return (
    <MainSection>
      <h1>Profile</h1>
      <p>Profile settings and role info will live here in upcoming iterations.</p>
    </MainSection>
  );
}
