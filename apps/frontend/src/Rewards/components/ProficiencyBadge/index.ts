// Public surface — consumers import from the directory and the dispatcher picks the variant.
export { ProficiencyBadge } from '@/Rewards/components/ProficiencyBadge/ProficiencyBadge';
export type { BadgeVariantProps } from '@/Rewards/components/ProficiencyBadge/types';
